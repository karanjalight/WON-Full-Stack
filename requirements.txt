# Python 3.11+ required. Use a virtual environment:
#   python3 -m venv venv && source venv/bin/activate
#   pip install -r requirements.txt

# Django core
Django==5.2.9
asgiref==3.11.0
sqlparse==0.5.4

# Admin & forms
django-jazzmin==3.0.1
django-ckeditor==6.7.1
django-js-asset==3.1.2
django-crispy-forms==2.5
crispy-bootstrap5==2025.6

# Utilities
python-decouple==3.8
Pillow==12.0.0
reportlab==4.2.5
chardet==5.2.0

# Production WSGI server (see deploy.md)
gunicorn==23.0.0

# PostgreSQL (only needed when USE_SQLITE=False in .env)
psycopg[binary]==3.2.9
